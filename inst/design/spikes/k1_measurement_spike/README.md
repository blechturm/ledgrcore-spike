# K1 Measurement Spike

Status: Stage 1 spec authored; maintainer review pending.

This directory holds the external `ledgrcore-spike` measurement specification
for ledgr's K1 compiled fold-core question. The binding sources are ledgr's
`inst/design/horizon.md` K1 entries and the v0.1.8.10 Round-3 substrate
decision that makes fold-owned FIFO accounting part of the post-substrate R
baseline.

Files:

- `spec.md`: binding measurement spec for the R, Rust, and C++ spike harness.
- `verdict.md`: deferred until measurements are complete.

Implementation must not proceed past this spec until maintainer review accepts
or revises the Stage 1 decisions.
